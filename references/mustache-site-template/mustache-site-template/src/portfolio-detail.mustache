<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="{{portfolio_item.summary}}" />
    <meta name="theme-color" content="#0a0a0a" />

    <!-- Open Graph -->
    <meta property="og:title"       content="{{portfolio_item.client}} — {{portfolio_item.category}} · {{site.company_name}}" />
    <meta property="og:description" content="{{portfolio_item.summary}}" />
    <meta property="og:image"       content="{{portfolio_item.cover}}" />
    <meta property="og:type"        content="website" />

    <title>{{portfolio_item.client}} — {{site.company_name}}</title>

    <!-- Preconnect for Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap"
          rel="stylesheet" />

    <!-- Tailwind CSS (CDN — replace with compiled bundle in production) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                        mono: ['"JetBrains Mono"', '"Courier New"', 'monospace'],
                    },
                },
            },
        };
    </script>

    <!-- Custom CSS -->
    <link rel="stylesheet" href="assets/css/main.css" />

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js" defer></script>
</head>

<body class="font-sans text-slate-800 bg-white antialiased">

<!-- ──────────────────────────────────────────────
     TOPBAR  (email · phone · social)
     ────────────────────────────────────────────── -->
{{> partials/public/common/topbar}}

<!-- ──────────────────────────────────────────────
     HEADER  (logo · nav · CTA)
     ────────────────────────────────────────────── -->
{{> partials/public/common/header}}

<!-- ──────────────────────────────────────────────
     MAIN CONTENT
     ────────────────────────────────────────────── -->
<main id="main">

    <!-- PAGE HERO (dark) — breadcrumb + title -->
    <section class="pt-32 pb-16 bg-[#0a0a0a] relative overflow-hidden">
        <!-- Background grid decoration -->
        <div class="absolute inset-0 opacity-5"
             style="background-image: linear-gradient(#16a34a 1px, transparent 1px), linear-gradient(90deg, #16a34a 1px, transparent 1px); background-size: 40px 40px;">
        </div>
        <div class="container mx-auto px-4 md:px-8 lg:px-12 relative">

            <!-- Breadcrumb (3 níveis) -->
            <nav class="flex items-center gap-2 text-sm font-mono mb-6" aria-label="Breadcrumb">
                <a href="{{page.breadcrumb.home.url}}" class="text-slate-500 hover:text-green-400 transition-colors duration-200">
                    {{page.breadcrumb.home.label}}
                </a>
                <i data-lucide="chevron-right" class="w-3 h-3 text-slate-600"></i>
                <a href="{{page.breadcrumb.parent.url}}" class="text-slate-500 hover:text-green-400 transition-colors duration-200">
                    {{page.breadcrumb.parent.label}}
                </a>
                <i data-lucide="chevron-right" class="w-3 h-3 text-slate-600"></i>
                <span class="text-green-400">{{page.breadcrumb.current}}</span>
            </nav>

            <div class="inline-flex items-center gap-2 text-green-400 text-sm font-mono font-medium mb-4 tracking-wider">
                <span class="text-green-500">>://</span>
                <span>{{page.hero.eyebrow}}</span>
            </div>
            <h1 class="text-4xl lg:text-5xl font-bold text-white mb-4">
                {{{page.hero.title}}}
            </h1>
            <p class="text-slate-400 text-lg max-w-2xl leading-relaxed">
                {{page.hero.subtitle}}
            </p>
        </div>
    </section>

    <!-- PORTFOLIO DETAIL CONTENT -->
    {{> partials/pages/portfolio_detail_full}}

</main>

<!-- ──────────────────────────────────────────────
     FOOTER
     ────────────────────────────────────────────── -->
{{> partials/public/common/footer}}

<!-- ──────────────────────────────────────────────
     SCRIPTS
     ────────────────────────────────────────────── -->
<script src="assets/js/main.js"></script>

</body>
</html>
